{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Docker against localhost",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "Run Docker Container",
      "platform": "node"
    }
  ]
}
